# 书签屏蔽与复习管理模块规划

## 一、功能概述

### 目标
为「书签推荐」增加两大核心能力：
1. **卡片交互** - 用户可对推荐卡片进行"跳过/稍后/屏蔽"操作
2. **屏蔽管理** - 提供统一的屏蔽规则配置界面

### 与现有功能的关系

```
书签推荐页面
├── A区：推荐公式（现有）
├── B区：推荐卡片（现有）← 底部增加交互按钮
│   └── 卡片底部：P值在左，[📌稍后] [⏭️跳过] [❌屏蔽] 在右
├── C区：稍后复习队列（新增）← 可折叠，热力图上方
├── D区：复习热力图（现有）
└── E区：屏蔽管理（新增）← 可折叠面板
    ├── 已屏蔽书签列表
    ├── 已屏蔽文件夹列表
    └── 已屏蔽域名列表
```

---

## 二、卡片交互设计

### 2.1 卡片布局调整

```
┌─────────────────────────────────────────┐
│                                         │
│         🌐 网站图标                      │
│                                         │
│     书签标题（最多2行）                   │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│   P = 0.75          [📌] [⏭️] [❌]      │  ← 底部：P值在左，按钮在右
│                                         │
└─────────────────────────────────────────┘
```

**布局说明：**
- P值在左侧，按钮在右侧，同一行
- 按钮小巧精致，不影响卡片整体美观
- 鼠标悬停时按钮显示文字提示

### 2.2 按钮功能定义

| 按钮 | 图标 | 名称 | 功能 | 说明 |
|:----:|:----:|------|------|------|
| 📌 | `fa-clock` | 稍后 | 推迟后再推荐 | 弹窗选择时间 |
| ⏭️ | `fa-forward` | 跳过 | 本次不显示 | 刷新页面后恢复 |
| ❌ | `fa-ban` | 屏蔽 | 永久不推荐 | 仅屏蔽此书签 |

### 2.3 交互流程

```
用户点击按钮
    │
    ├─→ [📌 稍后] → 弹出选择框
    │              ├─ 1小时后
    │              ├─ 明天
    │              ├─ 3天后
    │              └─ 1周后
    │              ↓
    │         记录 postponeUntil 时间
    │              ↓
    │         刷新卡片（换一张）
    │
    ├─→ [⏭️ 跳过] → 直接刷新这张卡片
    │              （本次会话内记住，刷新页面后重置）
    │
    └─→ [❌ 屏蔽] → 确认后直接屏蔽此书签
                   （无需选择，仅屏蔽单条）
                   ↓
              刷新卡片（换一张）
```

**注意：** 卡片上的屏蔽按钮仅屏蔽单个书签，文件夹/域名屏蔽在下方的「屏蔽管理」面板中配置。

---

## 三、屏蔽管理模块设计

### 3.1 数据结构

```javascript
// 存储在 chrome.storage.local
{
    // 永久屏蔽
    "recommend_blocked": {
        "bookmarks": ["bookmarkId1", "bookmarkId2"],     // 单条屏蔽
        "folders": ["folderId1", "folderId2"],           // 文件夹屏蔽
        "domains": ["example.com", "spam.site"]          // 域名屏蔽
    },
    
    // 稍后复习（惩罚/延迟机制）
    "recommend_postponed": [
        {
            "bookmarkId": "xxx",
            "postponeUntil": 1701388800000,  // 时间戳
            "postponeCount": 2,               // 被推迟次数
            "reason": "later"                 // later|skip
        }
    ],
    
    // 复习记录（简化版SM-2）
    "recommend_reviews": {
        "bookmarkId1": {
            "lastReview": 1701388800000,      // 上次复习时间
            "interval": 4,                     // 当前间隔(天)
            "reviewCount": 3,                  // 复习次数
            "ease": 2.5                        // 容易度因子
        }
    }
}
```

### 3.2 稍后复习队列UI（位于热力图上方，可折叠）

```
┌──────────────────────────────────────────────────────────────────┐
│  ⏰ 稍后复习 (3)                                           [▼]  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🔖 书签A    明天 09:30 (推迟2次)                  [✕ 取消]      │
│  🔖 书签B    后天                                  [✕ 取消]      │
│  🔖 书签C    3天后                                 [✕ 取消]      │
│                                                                  │
│  暂无稍后复习的书签                                              │ ← 空状态
└──────────────────────────────────────────────────────────────────┘
```

**设计说明：**
- 位于「复习热力图」上方，独立的可折叠区块
- 显示所有被"稍后复习"的书签
- 显示恢复时间和推迟次数
- 点击"取消"立即恢复到推荐池

---

### 3.3 屏蔽管理UI（位于热力图下方，可折叠）

```
┌──────────────────────────────────────────────────────────────────┐
│  🚫 屏蔽管理                                               [▼]  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─── 📑 已屏蔽书签 (3) ──────────────────────────────────────┐  │
│  │                                                            │  │
│  │  🔖 书签标题1                              [✕ 恢复]        │  │
│  │  🔖 书签标题2                              [✕ 恢复]        │  │
│  │  🔖 书签标题3                              [✕ 恢复]        │  │
│  │                                                            │  │
│  │  暂无已屏蔽书签                                            │  │ ← 空状态
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─── 📁 已屏蔽文件夹 (1) ────────────────────────────────────┐  │
│  │                                                            │  │
│  │  📁 工作/临时收藏 (包含12个书签)           [✕ 恢复]        │  │
│  │                                                            │  │
│  │  [+ 添加文件夹]  ← 弹窗选择书签树中的文件夹                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─── 🌐 已屏蔽域名 (2) ──────────────────────────────────────┐  │
│  │                                                            │  │
│  │  🌐 spam-site.com                          [✕ 恢复]        │  │
│  │  🌐 ads.example.com                        [✕ 恢复]        │  │
│  │                                                            │  │
│  │  [+ 添加域名]  ← 输入框手动添加                             │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│                                              [清空全部屏蔽]      │
└──────────────────────────────────────────────────────────────────┘
```

**设计说明：**
- 位于「复习热力图」下方，独立的可折叠区块
- 三个子区域：已屏蔽书签、已屏蔽文件夹、已屏蔽域名
- 每个子区域可独立展开/收起
- 文件夹和域名支持手动添加

---

## 四、推荐算法调整

### 4.1 过滤逻辑

```javascript
async function getRecommendableBookmarks() {
    const allBookmarks = await getAllBookmarks();
    const blocked = await getBlockedData();
    const postponed = await getPostponedData();
    const now = Date.now();
    
    return allBookmarks.filter(bookmark => {
        // 1. 检查单条屏蔽
        if (blocked.bookmarks.includes(bookmark.id)) {
            return false;
        }
        
        // 2. 检查文件夹屏蔽（包括父文件夹）
        if (isInBlockedFolder(bookmark, blocked.folders)) {
            return false;
        }
        
        // 3. 检查域名屏蔽
        const domain = getDomain(bookmark.url);
        if (blocked.domains.includes(domain)) {
            return false;
        }
        
        // 4. 检查是否在推迟期内
        const postponeInfo = postponed.find(p => p.bookmarkId === bookmark.id);
        if (postponeInfo && postponeInfo.postponeUntil > now) {
            return false;
        }
        
        return true;
    });
}
```

### 4.2 优先级惩罚

```javascript
function calculatePriority(bookmark, reviewData, postponeData) {
    // 基础优先级（现有公式）
    let priority = W1 * freshness + W2 * coldness + W3 * shallowRead + W4 * forgetting;
    
    // 惩罚因子：被多次推迟的书签降低优先级
    if (postponeData && postponeData.postponeCount > 0) {
        const penaltyFactor = Math.pow(0.9, postponeData.postponeCount);
        priority *= penaltyFactor;
        // 推迟2次 → 0.81, 推迟3次 → 0.73, 推迟5次 → 0.59
    }
    
    // 复习奖励：按时复习的书签略微提高优先级
    if (reviewData && reviewData.reviewCount > 0) {
        const reviewBonus = Math.min(reviewData.reviewCount * 0.02, 0.1);
        priority += reviewBonus;
    }
    
    return Math.min(priority, 1.0);
}
```

---

## 五、复习曲线（简化版SM-2）

### 5.1 复习触发

当用户**点击卡片打开书签**时，视为一次"复习"：

```javascript
async function onCardClick(bookmarkId) {
    // 打开书签
    chrome.tabs.create({ url: bookmark.url });
    
    // 记录复习
    await recordReview(bookmarkId);
    
    // 更新热力图
    await updateHeatmap(bookmarkId);
}

async function recordReview(bookmarkId) {
    const reviews = await getReviewData();
    const existing = reviews[bookmarkId] || {
        lastReview: 0,
        interval: 1,
        reviewCount: 0,
        ease: 2.5
    };
    
    // 简化版SM-2：每次复习间隔翻倍
    const newInterval = Math.min(existing.interval * 2, 30); // 最大30天
    
    reviews[bookmarkId] = {
        lastReview: Date.now(),
        interval: newInterval,
        reviewCount: existing.reviewCount + 1,
        ease: existing.ease
    };
    
    await saveReviewData(reviews);
}
```

### 5.2 基于复习状态的推荐

```javascript
function getReviewStatus(bookmark, reviewData) {
    if (!reviewData) return 'new';  // 从未复习
    
    const daysSinceReview = (Date.now() - reviewData.lastReview) / (1000 * 60 * 60 * 24);
    
    if (daysSinceReview >= reviewData.interval) {
        return 'due';      // 到期需要复习
    } else if (daysSinceReview >= reviewData.interval * 0.8) {
        return 'soon';     // 即将到期
    } else {
        return 'reviewed'; // 最近已复习
    }
}

// 在推荐时优先显示 due 和 soon 状态的书签
```

---

## 六、实现计划

### Phase 1：卡片按钮基础功能 ⭐ 优先实现
| 任务 | 文件 | 说明 |
|------|------|------|
| 卡片底部按钮UI | history.html, history.css | 3个按钮与P值同行 |
| "跳过本次"功能 | history.js | 内存存储，刷新后重置 |
| "屏蔽此书签"功能 | history.js | 单条屏蔽，存储到storage |
| 推荐过滤已屏蔽 | history.js | 刷新时排除已屏蔽书签 |

### Phase 2：稍后复习 & 弹窗
| 任务 | 文件 | 说明 |
|------|------|------|
| "稍后复习"弹窗 | history.html, history.css | 选择延迟时间 |
| 延迟数据存储 | history.js | postponeUntil + postponeCount |
| 推荐过滤延迟期 | history.js | 未到期的不推荐 |
| 惩罚因子计算 | history.js | 多次推迟降低优先级 |

### Phase 3：稍后复习 & 屏蔽管理面板
| 任务 | 文件 | 说明 |
|------|------|------|
| C区稍后复习队列UI | history.html, history.css | 热力图上方，可折叠 |
| 稍后复习列表展示 | history.js | 显示时间+推迟次数+取消 |
| E区屏蔽管理面板UI | history.html, history.css | 热力图下方，可折叠 |
| 已屏蔽书签列表 | history.js | 显示+恢复功能 |
| 文件夹屏蔽 | history.js | 选择器+批量屏蔽 |
| 域名屏蔽 | history.js | 输入框+手动添加 |

### Phase 4：复习曲线（可选）
| 任务 | 文件 | 说明 |
|------|------|------|
| 点击记录复习 | history.js | 打开书签=完成复习 |
| 复习间隔计算 | history.js | 简化版SM-2 |
| 复习状态标记 | history.js | 到期/即将到期/已复习 |

---

## 七、翻译文案

```javascript
// i18n 新增
{
    // 卡片按钮
    cardBtnLater: { zh_CN: '稍后复习', en: 'Review Later' },
    cardBtnSkip: { zh_CN: '跳过', en: 'Skip' },
    cardBtnBlock: { zh_CN: '屏蔽', en: 'Block' },
    
    // 稍后复习选项
    laterIn1Hour: { zh_CN: '1小时后', en: 'In 1 hour' },
    laterTomorrow: { zh_CN: '明天', en: 'Tomorrow' },
    laterIn3Days: { zh_CN: '3天后', en: 'In 3 days' },
    laterIn1Week: { zh_CN: '1周后', en: 'In 1 week' },
    
    // 屏蔽选项
    blockThisBookmark: { zh_CN: '屏蔽此书签', en: 'Block this bookmark' },
    blockThisFolder: { zh_CN: '屏蔽整个文件夹', en: 'Block entire folder' },
    blockThisDomain: { zh_CN: '屏蔽此域名', en: 'Block this domain' },
    
    // 屏蔽管理
    blockManageTitle: { zh_CN: '屏蔽管理', en: 'Block Management' },
    blockedBookmarks: { zh_CN: '已屏蔽书签', en: 'Blocked Bookmarks' },
    blockedFolders: { zh_CN: '已屏蔽文件夹', en: 'Blocked Folders' },
    blockedDomains: { zh_CN: '已屏蔽域名', en: 'Blocked Domains' },
    postponedQueue: { zh_CN: '稍后复习队列', en: 'Review Later Queue' },
    
    // 操作
    restore: { zh_CN: '恢复', en: 'Restore' },
    clearAll: { zh_CN: '清空全部', en: 'Clear All' },
    addDomain: { zh_CN: '手动添加域名', en: 'Add Domain' },
    
    // 状态
    postponedTimes: { zh_CN: '已推迟{n}次', en: 'Postponed {n} times' },
    reviewDue: { zh_CN: '待复习', en: 'Due for review' },
    reviewSoon: { zh_CN: '即将到期', en: 'Due soon' }
}
```

---

## 八、未来扩展

### 8.1 智能推荐（需要AI）
- 基于书签内容相似度去重
- 智能分类屏蔽建议
- 个性化复习间隔优化

### 8.2 同步与导出
- 屏蔽规则云同步
- 导出/导入屏蔽列表
- 与Anki格式互通

### 8.3 高级复习模式
- 5级评分（忘记/困难/一般/简单/太简单）
- 复习提醒弹窗
- 复习统计报告

---

## 九、文件修改清单

| 文件 | 修改内容 |
|------|---------|
| `history.html` | 卡片按钮、屏蔽管理面板、弹窗 |
| `history.css` | 按钮样式、面板样式、弹窗样式 |
| `history.js` | 屏蔽逻辑、延迟逻辑、复习记录、推荐过滤 |
| `background.js` | 可选：定时清理过期延迟数据 |

